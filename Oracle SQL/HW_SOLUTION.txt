SQL> ---DISPLAY NUMBER OF DEPT IN EMP TABLE
SQL> SELECT COUNT(DISTINCT DEPTNO) FROM EMP;

COUNT(DISTINCTDEPTNO)
---------------------
                    4

SQL> ---DISPLAY NAME OF DEPT WITH NUMBER OF EMPLOYEES IN EMP TABLE
SQL> SELECT D.DNAME, COUNT(*) FROM EMP E, DEPT D
  2  WHERE E.DEPTNO=D.DEPTNO
  3  GROUP BY D.DNAME;

DNAME            COUNT(*)
-------------- ----------
RESEARCH                5
SALES                   6
ACCOUNTING              2
OPERATIONS              1

SQL> SELECT ENAME FROM EMP WHERE SAL=(
  2  SELECT MIN(F.SAL) FROM EMP E, EMP F WHERE E.MGR=F.EMPNO);

ENAME
----------
CLARK

SQL> ---DISPLAY NAME OF EMPLOYEE WHOSE SALARY BELONGS TO 2ND LOWEST SALARY
SQL> SELECT ENAME FROM EMP WHERE SAL=(
  2  SELECT MIN(SAL) FROM EMP WHERE SAL > (
  3  SELECT MIN(SAL) FROM EMP));

ENAME
----------
JAMES

SQL> ---DISPLAY NAME OF EMPLOYEE WHOSE SALARY BELONGS TO 2ND HIGHEST SALARY
SQL> SELECT ENAME FROM EMP WHERE SAL=(
  2  SELECT MAX(SAL) FROM EMP WHERE SAL < (
  3  SELECT MAX(SAL) FROM EMP));

ENAME
----------
SCOTT
FORD

SQL> ---DISPLAY NUMBER OF EMPLOYEES IN EACH JOB
SQL> SELECT JOB, COUNT(*) FROM EMP GROUP BY JOB;

JOB         COUNT(*)
--------- ----------
CLERK              4
SALESMAN           4
ANALYST            2
MANAGER            3
PRESIDENT          1

SQL> ---DISPLAY NUMBER OF DUPLICATE SALARAY
SQL> SELECT COUNT(*) FROM (
  2  SELECT SAL, COUNT(*) FROM EMP GROUP BY SAL HAVING COUNT(*) > 1);

  COUNT(*)
----------
         2
