DECLARE
	TYPE T IS RECORD
	(
		X EMP.ENAME%TYPE,
		Y EMP.SAL%TYPE,
		Z EMP.JOB%TYPE
	);
	R T;
BEGIN
	SELECT ENAME, SAL, JOB INTO R FROM EMP WHERE EMPNO=7788;
	DBMS_OUTPUT.PUT_LINE(R.X||' '||R.Y||' '||R.Z);
END;
/