DECLARE
	TYPE T IS TABLE OF EMP%ROWTYPE INDEX BY BINARY_INTEGER;
	R T;
BEGIN
	SELECT * INTO R(0) FROM EMP WHERE EMPNO=7788;
	DBMS_OUTPUT.PUT_LINE(R(0).ENAME||' '||R(0).SAL||' '||R(0).JOB);
	
	SELECT * INTO R(1) FROM EMP WHERE EMPNO=7369;
	DBMS_OUTPUT.PUT_LINE(R(1).ENAME||' '||R(1).SAL||' '||R(1).JOB);
END;
/